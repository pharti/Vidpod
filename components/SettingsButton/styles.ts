import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    text:{
        color:'white'
    },
    container:{
        paddingRight:10,
        paddingLeft: 10
    }

})

export default styles 